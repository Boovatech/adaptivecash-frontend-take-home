# @adaptivecash/design-tokens

Single source for AdaptiveCash colors, typography, spacing, density, elevation,
status/risk tokens and z-index values.

Feature modules must not define raw brand/status colors. Tailwind configuration
should consume these tokens when Tailwind is introduced.
