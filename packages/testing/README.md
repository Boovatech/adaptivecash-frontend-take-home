# @adaptivecash/testing

Shared test fixtures, visual/a11y helpers and mock API data for frontend modules.

`createMockRequestsApi` is the ready-to-use asynchronous API for the frontend
candidate assignment. All records are synthetic. The controller exposes
deterministic failures and signing outcomes for UI tests without requiring a
mock server or changes to production components.
