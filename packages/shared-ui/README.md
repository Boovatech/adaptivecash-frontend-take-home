# @adaptivecash/shared-ui

Framework-facing primitive UI wrappers.

Primary UI is Fluent UI React. FAST / Fluent Web Components may appear here only
behind typed wrappers that normalize props, events, theming, focus/a11y behavior
and tests. Feature modules must not import FAST/Web Components directly.
